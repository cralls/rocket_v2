<?php
if(isset($_COOKIE['msg_id'])) {
    echo "<!-- Custom Request https://www.rocketsciencesports.com/custom.php?s=msg_id&msg_id=".$_COOKIE['msg_id']."-->";
    $ch = curl_init();
    curl_setopt($ch, CURLOPT_URL, "https://www.rocketsciencesports.com/custom.php?s=msg_id&msg_id=".$_COOKIE['msg_id']);
    curl_setopt($ch, CURLOPT_HEADER, 0);
    curl_exec($ch);
    curl_close($ch);
}
?>
